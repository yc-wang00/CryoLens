# Components Module

## Purpose

Reusable frontend presentation components for CryoSight.

## Rules

- Prefer small composable display primitives over page-local one-off markup.
- Keep Ask-specific telemetry UI secondary to the answer content.
- Reuse existing UI primitives in `components/ui` before introducing new styling systems.
