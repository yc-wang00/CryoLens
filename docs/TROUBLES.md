2026-03-22 10:43 UTC | ask,prompting,research | make the sandbox outputs interesting and useful for researcher-style hero-story questions | broad hero-story prompts caused tool-chasing, search narration, and overconfident recommendations; early versions also turned “not retrieved” into “not present” | use narrow decision-shaped prompts, explicit early-stop guidance, and separate retrieved facts from inference with a counterpoint section | encode those rules in the system prompt and preserve them as memory
2026-03-22 11:40 UTC | ask,local-dev,routing | get the frontend research agent running locally without more bug hunting | local Ask debugging kept drifting into FastAPI and `vercel dev` routing issues, including 404/431 confusion and a linked-project mismatch between the frontend app and root API route | give Ask a dedicated local API server that reuses the sandbox handler, point the browser at `VITE_AGENT_API_BASE_URL`, and keep local CORS/logging explicit | treat local Ask as its own dev service and stop using `vercel dev` as the primary debug path
2026-03-22 12:38 UTC | ask,env,override | get a live demo working without another fetch failure | the frontend kept bypassing the working Vercel route because `VITE_AGENT_API_BASE_URL` still pointed at the dead local `127.0.0.1:3210` server | default demos to same-origin `/api/agent-search`, keep `VITE_AGENT_API_BASE_URL` blank unless you intentionally want another backend, and add client fallback when the override is unreachable | treat browser API base overrides as opt-in and verify them before demoing
