2026-03-22 10:43 UTC | RULE | MEM-0011 | ask,research,prompt | CryoSight Ask outputs are materially better when framed as narrow researcher decisions with an explicit tool budget and early-stop rule; broad “cover everything” prompts tend to over-search and under-synthesize.
2026-03-22 10:43 UTC | RULE | MEM-0012 | ask,research,trust | Ask answers must distinguish retrieved facts from inference, use “I did not retrieve direct evidence” instead of absolute absence claims, and label proposed formulation changes as hypotheses to test unless direct head-to-head evidence exists.
2026-03-22 10:43 UTC | RULE | MEM-0013 | ask,sessions,sandbox | Claude Agent SDK session resume/fork will not work across jobs if the Vercel sandbox is destroyed after each run, because the session files persist inside the sandbox filesystem rather than on the host.
2026-03-22 11:40 UTC | RULE | MEM-0014 | ask,local-dev,vercel | Local Ask development should use the standalone agent API server (`pnpm run dev:agent-api` or `pnpm run dev:ask`) rather than relying on `vercel dev` request routing; the deployed Vercel function remains correct, but the linked frontend project and Vite topology make `vercel dev` a brittle local test path.
2026-03-22 11:48 UTC | RULE | MEM-0015 | ask,api,cors | `/api/agent-search` is a cross-origin browser API and must answer `OPTIONS` plus return CORS headers on both JSON and SSE responses so non-primary UIs can stream Ask output directly.
2026-03-22 12:38 UTC | RULE | MEM-0016 | ask,api,vercel | The default demo/public Ask path is same-origin Vercel `/api/agent-search`; `VITE_AGENT_API_BASE_URL` is only an override, and the browser client should fall back to same-origin when that override is unreachable.
